# Sequence Homology Search

Searches one or more Primify reference genomes with the bundled BLAST+ runtime.

Results include ranked sequence matches, genomic coordinates, overlapping or nearest genes, transcript IDs, pairwise alignment, a coverage plot, and a plain-text report. Reference genomes and query sequences remain on the user's computer.

Requires Primify 0.1.64 or newer.
